let isCombatMusicEnabled = false;

export function getCombatMusicEnabled() {
  return isCombatMusicEnabled;
}

export function setCombatMusicEnabled(isEnabled: boolean) {
  isCombatMusicEnabled = isEnabled;
}
